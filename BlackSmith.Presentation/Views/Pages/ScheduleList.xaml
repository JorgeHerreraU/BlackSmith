<Page
    x:Class="BlackSmith.Presentation.Views.Pages.ScheduleList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewModels="clr-namespace:BlackSmith.Presentation.ViewModels"
    Title="ScheduleList"
    d:DataContext="{d:DesignInstance viewModels:ScheduleListViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:CardAction
            Grid.Row="0"
            Margin="0,0,4,0"
            Command="{Binding GoToCreate}"
            Icon="AddCircle24"
            ShowChevron="False"
            Tag="create">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,4"
                    FontWeight="Medium"
                    Text="Create" />
                <TextBlock
                    FontSize="11"
                    Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                    Text="Create a new schedule" />
            </StackPanel>
        </ui:CardAction>
    </Grid>
</Page>