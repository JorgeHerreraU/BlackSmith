<window:AdonisWindow  x:Class="BlackSmith.Presentation.MainWindow"
                      xmlns:window="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:BlackSmith.Presentation.Controls"
        xmlns:viewmodels="clr-namespace:BlackSmith.Presentation.ViewModels"
        xmlns:views="clr-namespace:BlackSmith.Presentation.Views"
        mc:Ignorable="d"
        Title="MainWindows" Height="768" Width="1024"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <controls:Navbar Grid.Column="0" DataContext="{Binding Navigator}" />
        <ContentControl Grid.Column="1" Content="{Binding Navigator.CurrentViewModel}" Margin="10" >
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
                    <views:HomeView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:AppointmentViewModel}">
                    <views:AppointmentView />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</window:AdonisWindow>
